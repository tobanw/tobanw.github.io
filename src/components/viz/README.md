# Visualization Islands

Future interactive visualizations should live here as React components and be mounted from Astro pages with `client:visible` or `client:idle`.

Keep data parsing and chart state inside the island. Keep surrounding page copy, metadata, and navigation as static Astro content.
